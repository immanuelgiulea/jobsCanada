[project]
name = "jobs"
version = "0.1.0"
description = "Canonical Canadian NOC 2021 and OaSIS labour-market dataset and static site"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

# Legacy LLM Scoring Archive

This directory preserves the pre-EPIAC OpenRouter scoring experiment and cached output.

Nothing in this directory is imported by the active Canadian build, runtime product, or generated artifacts.
